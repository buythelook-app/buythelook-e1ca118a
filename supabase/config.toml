project_id = "aqkeprwxxsryropnhfvm"

[functions.generate-outfit]
verify_jwt = false
cors = true
# Avoid rate limits with higher limits
rate_limits = [
  { method = "POST", count = 100, duration = 60 }
]

[functions.trainer-agent]
verify_jwt = false
cors = true

# Cron job configuration for trainer-agent
[[functions.trainer-agent.schedules]]
name = "quarter-hourly-trainer-validation"
schedule = "*/15 * * * *"  # Run every 15 minutes
path = "/"
method = "POST"
headers = { "Content-Type" = "application/json" }
body = "{}"

[functions.serp-search]
verify_jwt = false
cors = true

[functions.fashion-items]
verify_jwt = false
cors = true
